# PRISM Autonomous SOC Agent Configuration

# Decision Engine Provider: "gemini" (LIVE mode) or "deterministic" (MOCK mode)
LLM_PROVIDER=gemini

# Google Gemini API Key (Required for LIVE mode)
GEMINI_API_KEY=

# Model Name
LLM_MODEL=gemini-1.5-flash
